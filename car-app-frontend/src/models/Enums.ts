export enum Transmission{
    ALL = 0,
    MANUAL = 1,
    AUTOMATIC = 2
}

export enum CarBody{
    ALL = 0,
    COUPE = 1,
    CONVERTIBLE = 2,
    HATCHBACK = 3,
    SEDAN = 4,
    SUV_CROSSOVER = 5,
    TRUCK = 6,
    VAN_MINIVAN = 7,
    WAGON = 8
}

export enum DriveTrain {
    REAR = 0,
    FRONT = 1,
    BOTH = 2
}

export enum UserType{
    PERSONAL = 0,
    COMPANY = 1
}